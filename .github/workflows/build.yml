name: Build

on:
  push:
    branches:
    - 'master'
  pull_request:
    branches:
    - 'master'

env:
  CARGO_UNSTABLE_SPARSE_REGISTRY: 'true'

jobs:
  build:
    name: specr build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: bash ci.sh
